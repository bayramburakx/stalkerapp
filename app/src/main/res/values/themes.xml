<?xml version="1.0" encoding="utf-8"?>
<resources>
    <style name="Theme.StalkerApp" parent="android:Theme.Material.NoActionBar">
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/black</item>
        <item name="android:windowBackground">@color/black</item>
    </style>

    <!-- Android 12+ splash: markalı ikon, siyah zemin, ardından uygulama temasına geçer. -->
    <style name="Theme.StalkerApp.Starting" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/black</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/ic_splash</item>
        <item name="postSplashScreenTheme">@style/Theme.StalkerApp</item>
    </style>
</resources>
